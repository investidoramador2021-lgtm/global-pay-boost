# Node.js Lite API Bot

ESM example using native `fetch` (Node 18+). Same estimate → create → poll flow as the Python sibling.

## Run

```bash
node swap-bot.mjs
```

No `npm install` needed — uses only Node built-ins.

Edit the constants at the top of `swap-bot.mjs` for your pair / recipient wallet.
