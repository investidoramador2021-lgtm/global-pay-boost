<?xml version="1.0" encoding="UTF-8"?>
<!--
  Sitemap index for mrcglobalpay.com.
  This is a tiny static stub that delegates to the dynamic-feed edge function,
  which auto-generates the full child list based on the live pair count
  (sync_engine_state.pairs_count) so it stays in sync as the pair table grows.
  Cross-host children inside a sitemap index are accepted by Google/Bing as
  long as the host of THIS index file (mrcglobalpay.com) is verified in
  Search Console — no need to verify the supabase.co host.
-->
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <sitemap>
    <loc>https://tjikwxkmsfmyjkssvyoh.supabase.co/functions/v1/dynamic-feed?p=/</loc>
  </sitemap>
</sitemapindex>
